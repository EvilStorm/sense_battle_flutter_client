import 'package:flutter/material.dart';

class ThridPartySignInSection extends StatelessWidget {
  const ThridPartySignInSection({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}